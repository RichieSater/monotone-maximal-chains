name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  gap:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify p=3,5,7 with digest-pinned GAP 4.11.1
        env:
          GAP_BIN: /nonexistent
        run: make smoke counterexample
